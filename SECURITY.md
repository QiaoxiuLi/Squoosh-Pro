# Security Policy

Please report security issues privately to the repository owner rather than opening a public issue. Do not include credentials, private images, or sensitive metadata in a report.

Security-sensitive areas include path collision checks, symlink handling, atomic commits, preset import validation, WebKit navigation policy, and codec memory limits. The application is designed without network client or server entitlements and never needs account credentials.
